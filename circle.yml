machine:
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.2.2

#dependencies:
#  override:
#    - docker run busybox echo hello world

test:
  override:
    - docker run busybox echo hello world
    - bundle exec rspec spec/
