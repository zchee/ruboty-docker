require 'spec/spec_helper'

describe Ruboty::Docker do
  it 'has a version number' do
    expect(Ruboty::Docker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
